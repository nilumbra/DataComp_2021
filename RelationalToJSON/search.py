from collections import namedtuple 
Search = namedtuple('Search', ['word', 'create_at'])
#class Search(search):
    # def __init__(self, search):
    #     word = search[0]
    #     created = search[1]

    
#    def __new__(cls, search):
#        return super(cls, Search).__new__(cls, search[0], search[1]) 
    #def is_a_night_comment():

#print(Search((1,2)))